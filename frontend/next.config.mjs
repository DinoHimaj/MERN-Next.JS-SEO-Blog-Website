/** @type {import('next').NextConfig} */
const nextConfig = {
  // Remove runtime config in favor of environment variables
  // Other Next.js config options can stay here
};

export default nextConfig;
